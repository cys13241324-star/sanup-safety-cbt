# 빌드 설명서

원고에서 이 저장소의 산출물을 다시 만드는 법. 사람이 손으로 고친 자리는 없다.
`build/` 의 스크립트가 전부이고, 산출물은 언제든 지웠다가 다시 만들 수 있다.

---

## 0. 먼저 알아야 할 것 — 원고는 이 저장소에 없다

```
D:\project\산업안전기사\산업안전기사\      ← 원고. 저장소 밖에 있다
  src\past\R01_S1.ps1 ~ R24_S6.ps1        본문·해설 (144개)
  기출_재배열\R01.json ~ R24.json          분류·출제이력·점검표시
  src\fig\*.png                            그림
```

**이 저장소를 clone 해도 다시 빌드할 수 없다.** 원고 폴더가 있어야 한다.
다른 컴퓨터에서 원고를 고쳤다면, 그 컴퓨터의 원고를 먼저 가져와야 한다.

> ⚠ **가장 잘 저지르는 사고** — 원고가 예전 판인 컴퓨터에서 `build_all.py` 를 돌려
> 최신 산출물을 덮어쓰는 것. 커밋 로그에는 「원고 최신본 반영」이 남아 있는데
> 내 컴퓨터의 원고는 그 전 판이면, 다시 빌드하는 순간 그 작업이 통째로 날아간다.
>
> 빌드 전에 반드시 확인한다.
> ```
> python build/verify.py
> ```
> 「그림 개수 … 기대 21 실제 23」처럼 **실제가 기대보다 많으면 내 원고가 뒤처진 것**이다.
> 이때는 빌드하지 말고 원고부터 맞춘다.

경로는 `dataset.py` 의 `pick()` 이 골라 준다. `D:\project\…` 가 없으면
`~\Desktop\project\산업안전기사` 를 본다. 컴퓨터가 다르면 여기에 자리를 더 적는다.

---

## 1. 갖춰야 할 것

| | |
|---|---|
| Python 3.11+ | `pip install openpyxl playwright` |
| Node.js | KaTeX 렌더 검사에만 쓴다 |
| 브라우저 | 화면 확인용. `playwright install chromium` |
| 인터넷 | `law_fetch.py` 를 돌릴 때만 (법제처 조회) |

한글(HWP)은 **필요 없다.** 이 저장소는 웹 산출물만 만든다.
hwpx 조판은 다른 저장소(`sanup-safety-engineer`)의 일이다.

---

## 2. 실행 순서

```bash
cd D:\safety-cbt

python build/law_fetch.py      # ① 법령 원문 받기 (인터넷). 한 번 받아 두면 됨
python build/build_all.py      # ② 전체 산출물
python build/verify.py         # ③ 점검 — 여기서 「문제 없음」이 나와야 한다

# 더 볼 것
python build/collect_math.py && node build/katex_check.js   # 수식 전수 렌더
python build/shot_study.py     # 학습 화면을 실제로 눌러 보기
python build/shot_cbt.py       # 시험 화면
python build/lawdiff.py        # 출제 당시 기준 ↔ 현행 조문 대조
```

②만 3~5분, 나머지는 각 1분 안쪽이다.

---

## 3. 스크립트가 하는 일

읽는 순서대로 적는다. 아래로 갈수록 위의 것을 쓴다.

| 파일 | 하는 일 |
|---|---|
| `psparse.py` | ps1 원고의 `$QS += @{…}` 해시테이블 리터럴 파서. 백틱 이스케이프·`""` 처리 |
| `dataset.py` | 원고 + 재배열 메타를 문항 한 줄로 합친다. 빈출도·난이도·문제유형 산출, 빠진 분류 메우기 |
| `hwpeq.py` | 한글 수식편집기 스크립트 → LaTeX (`over`→`\dfrac`, 한글 낱말→`\text{}`) |
| `render.py` | 원고 표기 → HTML. 강조·수식·정리표·오답분석·학습포인트, 조사 붙이기 |
| `cards.py` | 같은 개념을 묻는 문항을 묶어 암기카드로 |
| `law_fetch.py` | 법제처 DRF 에서 시행 중인 법령 12종 조문 수집 → `build/_laws.json` |
| `lawcite.py` | 원고의 법령 인용을 읽어 조문 원문을 붙이고, 묻는 자리에 형광·표시 |
| `cbt_app.py` | 학습 화면·시험 화면 HTML (한 파일로 도는 앱) |
| `card_view.py` | 플립카드 뷰어 |
| `build_all.py` | 위를 다 엮어 회차 폴더·엑셀·인덱스를 쓴다 |
| `verify.py` | 산출물 점검. **만들어 놓고 믿지 않는다** |
| `lawdiff.py` | 정답 수치가 현행 조문과 어긋나는지 대조 |
| `collect_math.py` · `katex_check.js` | 수식 1,481개를 KaTeX 로 실제 렌더해 본다 |
| `shot.py` · `shot_cbt.py` · `shot_study.py` | 브라우저로 실제로 눌러 보고 화면을 찍는다 |

---

## 4. 원고의 어느 칸이 어디로 가는가

원고 문항 하나(`$QS += @{…}`)의 필드가 이렇게 흩어진다.

| 원고 | 산출물 |
|---|---|
| `t` | 발문 |
| `c` · `a` | 보기 1~4 · 정답 |
| `qb` | 조건(제시문 상자) |
| `qfig` | 발문그림 → `img/{코드}_stem1.png` |
| `sol` | 해설 |
| `tbl` | 해설 안 정리표 |
| `fig` · `figcap` | 해설그림 → `img/{코드}_exp1.png` |
| `w` | 오답분석 — 정답을 뺀 보기 번호에 차례로 붙는다 |
| `kn` · `key` | 학습포인트(관련개념) + **법령 인용**(대시 앞) |
| `mem` | ◈ 암기 |
| `old` | ⚠ 주의사항 |
| `topic` | 분류 「내용」 |
| `src` | 원본 시행일 → 빈출도 산출 근거, 비고 |

재배열 메타(`R##.json`)에서는 `big`·`small`(분류), `hist`(출제이력),
`suspect`·`crossed`·`needfig`·`revised`(점검표시)를 가져온다.

**조인 키는 json 의 `no` ↔ ps1 의 `n`** 이다. json 의 `n` 은 원본 시행회차 안에서의
번호라 맞지 않는다. 이걸 틀리면 전 문항의 분류가 어긋난다.

---

## 5. 함정 모음

실제로 겪은 것만 적는다.

**원고 표기**
- `==형광==` `__밑줄__` `**굵게**` `~~연두~~` `#=수식=#` `#{수량}#` — `lib.ps1` 의
  `Put-Rich` 와 같은 규칙이다. 대안 순서까지 같게 두어야 한다.
- `#{A}##{B}#` 처럼 수식이 둘 붙어 있는 자리가 있다. 앵커(`^…$`)로 「토막 전체가
  수식 하나」를 판정하면 역추적이 일어나 둘을 하나로 삼킨다. 낱낱이 세야 한다.
- 원고는 **강조 뒤 조사를 띄어 적는다**(한글 조판에서 조사까지 굵어지는 것을 피하려고).
  HTML 에서는 붙여야 한다 → `render.tighten()`. 다만 「것이·때문이다·한다·세 가지」는
  띄는 것이 맞으니 조사 목록에서 뺀다.

**수식**
- 한글 수식은 라틴 글자를 이탤릭으로 찍는다. 단위·원소기호(mA, kg, Al)와 세 글자 이상
  약어(MTBF)만 정체로 세우고, 두 글자 대문자(CV, EI, AB)는 변수의 곱이므로 이탤릭에 둔다.
- `<` `>` 가 든 LaTeX 를 그냥 넣으면 브라우저가 태그로 읽는다. 반드시 실체참조로.

**법령**
- `target=law` 로 받으면 **공포 기준** 최신본이 와서 아직 시행되지 않은 개정이 섞인다.
  반드시 `target=eflaw` 로 시행일 목록을 받아 **오늘 이하의 가장 늦은 시행일**을 고른다.
- 조문내용의 첫 줄은 「제139조(제목)」이라 조문 제목과 겹친다. 걷어내되, 그 줄이 본문의
  전부인 조문은 손대면 내용이 통째로 사라진다.
- 원고의 인용이 어긋난 자리가 있다. 「안전보건규칙 제79조 — 협의체의 구성 및 운영」인데
  그 조문은 **시행규칙** 제79조다. 그냥 두면 휴게시설 조문이 붙는다.
  대시 뒤 개념명과 조문 제목을 맞춰 바로잡는다(`lawcite.resolve`).
- 원고는 「30cm」, 법령은 「30센티미터」로 적는다. 단위를 서로 바꿔 가며 찾아야 한다.

**개정 대조**
- **「틀린 것은?」 유형은 정답 보기가 법과 어긋나는 것이 정상이다.** 조문이 붙은
  287문항 중 153문항이 그 유형이다. 이를 걸러 내지 않으면 멀쩡한 문항이 무더기로
  「어긋남」으로 잡힌다(실제로 18건이 거짓으로 잡혔다). 발문의 부정 표현을 먼저 가린 뒤
  긍정형 문항의 **정답 보기 수치만** 대조한다.

**정리표**
- 셀 합치기가 없어 빈칸으로 둔 자리가 있다. 통째로 빈 열은 버리고, **왼쪽 분류 열에서
  아래로 이어지는 빈칸만** `rowspan` 으로 합친다. 비고처럼 그냥 값이 없는 칸까지
  합치면 뜻이 달라진다.

**엑셀**
- 등록양식(v3)이 허용하는 태그는 `<i> <em> <strong> <u> <sub> <sup> <br> <code>` 와
  블록 몇 개뿐이다. `<mark>` `<div>` `<details>` 는 못 쓴다. 웹 화면과 엑셀은
  같은 내용을 다른 태그로 낸다(형광 → `<strong>`, 자리 표시 → `▶`).

**도구**
- 이 환경의 Bash heredoc 은 따옴표를 씌워도 백슬래시를 한 겹 벗긴다. 정규식·LaTeX·
  윈도 경로가 든 파일을 heredoc 으로 쓰면 조용히 망가진다. 코드 파일은 파일 쓰기 도구로.

---

## 6. 산출물

```
_index.html                      시작점 — 회차 고르기
_암기카드.html                    플립카드 1,966장
_index_검수본.html                검수본 인덱스
index.html · .nojekyll           GitHub Pages 용 (Jekyll 은 밑줄 파일을 무시한다)
CBT_2026_1회 ~ CBT_2021_4회/
  ├ {연도}_{회}회_학습.html         해설 토글 · 암기카드 팝업 · 근거 법령
  ├ {연도}_{회}회_CBT.html          3시간 타이머 · 제출 채점
  ├ {연도}_{회}회_검수본.html        지면 대조용
  ├ 산업안전기사_{연도}_{회}회_문항등록.xlsx
  └ img/ · katex/
산업안전기사_CBT_24회차_통합.xlsx    2,880문항
산업안전기사_암기카드_1966장.xlsx
build/_laws.json                 법령 원문 (다시 받을 수 있다)
```

회차 폴더의 `katex/` 는 24벌이지만 파일이 같아 git 은 한 벌만 저장한다.

---

## 7. 자주 하는 일

**문항 하나를 고쳤다**
```bash
python build/build_all.py && python build/verify.py
```
원고만 고치면 된다. 산출물은 손대지 않는다.

**법령이 개정됐다**
```bash
python build/law_fetch.py     # 시행일이 바뀐 것만 다시 받는다
python build/build_all.py
python build/lawdiff.py       # 정답이 어긋나게 된 문항이 있는지
```
`law_fetch.py` 는 받을 때 **시행 전 개정**도 함께 찍는다.
「(시행 전 개정 20261208, …)」이 보이면 그날 이후 다시 받아야 한다.

**회차를 늘린다**
원고에 `R25_S1.ps1` 같은 식으로 넣고 `dataset.YEARS` 에 해를 더한다.
지금은 4회차씩 한 해로 묶어 2026 → 2021 순으로 내려간다.

**GitHub Pages 에 올린다**
`main` 에 push 하면 1~2분 뒤 자동으로 반영된다.
https://cys13241324-star.github.io/sanup-safety-cbt/

---

## 8. 점검이 보는 것

`verify.py` 가 통과라고 말할 때 실제로 확인한 것들.

- 정답 2,880문항 전수 — 원고(ps1)와 재배열 메타(json)를 **서로 다른 경로로 만들어진
  두 파일**로 교차검증한다. 불일치 0.
- 회차별 120문항 · 문항코드 유일 · 필수열 빈칸 0 · 관련카드ID 100 %
- 카드 링크 5,760개가 실제로 있는 카드를 가리키는가
- 학습·시험 화면이 각각 제 모드로 만들어졌는가
- 그림 파일 수가 원고와 맞는가 ← **원고가 뒤처졌는지 여기서 드러난다**

발문 글자 불일치 25건은 결함이 아니다. 원고 저자가 CBT 사이트의
「문제 오류로 가답안…」 안내문을 떼어내고 따옴표를 「」로 고친 자리다.
